// 1. In JS using semi-colon is optional. you may may not write but its advisable to use it as it makes code more readable

const piValue = 3.17
const myName = "meghsham";

// 2. "use script" treats all JS code as newer version with file

// 3. there are some function which only executes under browser like alert() but will give error in node environment and vice-versa

// 4. JS coding standards are defined by ECMA_Script and ECMA_Script is detailed doc for JS