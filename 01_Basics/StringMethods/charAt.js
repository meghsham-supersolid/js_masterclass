let message = "Have A Great Day Ahead";

for (let index = 0; index < message.length; index++) {
    
    console.log(
      `Character ${index} in Message String ${message} is '${message.charAt(
        index
      )}'`
    );
}
