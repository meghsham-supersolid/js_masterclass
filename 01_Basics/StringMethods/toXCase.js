let str = "Lenovo ThinkPad ";

console.log(
  `Using toLowerCase() we can convert all letter to lower case only ${str.toLowerCase()}`
);

console.log(
  `Using toUpperCase() we can convert all letter to upper case only ${str.toUpperCase()}`
);
