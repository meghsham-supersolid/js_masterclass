// Function is block of code which can used to do repetitive work

// Step 1 : declare a function with it parameter i.e. things that it takes as input
//  Syntax : function functionName (parameter1, parameter2)

// Step 2 : define function body e.g. a group of statement which does some work
//  function functionName (parameter1, parameter2) {
//      body
// }

// Step 3 : return a value after function execution
//  function functionName (parameter1, parameter2) {
//      //does some work
//      return someValue; //you  can just return without any value if you don't have anything to return
//      what every you write after return statement is not executable as control is return to caller and after this nothing executes
// }

// Step 4  : call the function to execute with arguments e.g. values that function can take as a parameter

// if the function returns some value them you must save it or pass that value to further processing
// but if the function return nothing but you still expecting a value from it, you will get undefined

// as per clean code standards, write function in snakeCase() 