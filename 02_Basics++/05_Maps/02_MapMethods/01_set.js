/* 
mapName.set(key , value) used to add or update entry
    1. if key already does not exist then it create entry
    2. if key already exist then it just update entry
*/
const myMap = new Map();
console.log(myMap);

myMap.set("fistname", "meghsham");
console.log(myMap);
myMap.set("fistname", "Meghsham Kapure");
console.log(myMap);
