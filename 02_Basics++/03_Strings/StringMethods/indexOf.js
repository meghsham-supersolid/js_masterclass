let str = "The quick brown fox jumps over the lazy dog";

console.log(
  `indexOf() gives the first occurrence of given string ${str.indexOf("a")}`
);

console.log(
  `indexOf() gives the first occurrence of given string ${str.indexOf("b")}`
);

console.log(
  `indexOf() gives the first occurrence of given string ${str.indexOf("c")}`
);

console.log(
  `indexOf() gives the first occurrence of given string ${str.indexOf("d")}`
);

console.log(
  `indexOf() gives the first occurrence of given string ${str.indexOf("e")}`
);
